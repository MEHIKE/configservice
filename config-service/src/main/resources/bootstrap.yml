server.port: 8088
spring.config.name: configserver
#spring.cloud.config.server.git.uri: file://${user.home}/config-repo
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MEHIKE/config-repo.git
          skipSslValidation: true